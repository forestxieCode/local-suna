# ==================================================================
# Kortix - Tencent Cloud Configuration Example
# ==================================================================
# This file shows how to configure Kortix to use Tencent Cloud services
# Copy this to .env and fill in your credentials
# ==================================================================

# ==================== Cloud Provider Selection ====================
CLOUD_PROVIDER=tencent  # Options: aliyun | tencent | local | supabase

# ==================== Tencent Cloud Credentials ====================
# Get these from Tencent Cloud Console: https://console.cloud.tencent.com/cam/capi
TENCENT_SECRET_ID=your_secret_id
TENCENT_SECRET_KEY=your_secret_key
TENCENT_REGION=ap-guangzhou  # Options: ap-guangzhou, ap-shanghai, ap-beijing, etc.

# ==================== Database (TDSQL-C / PostgreSQL) ====================
# Tencent Cloud TDSQL-C or PostgreSQL Configuration
TENCENT_RDS_HOST=your-tdsql-instance.sql.tencentcdb.com
TENCENT_RDS_PORT=5432
TENCENT_RDS_DATABASE=kortix
TENCENT_RDS_USERNAME=kortix_user
TENCENT_RDS_PASSWORD=your_secure_password

# Optional: Read Replica for better performance
# TENCENT_RDS_READ_HOST=your-read-replica.sql.tencentcdb.com

# ==================== Object Storage (COS) ====================
# Tencent Cloud COS Configuration
TENCENT_COS_REGION=ap-guangzhou
TENCENT_COS_BUCKET=kortix-files-1234567890  # Format: bucketname-appid
TENCENT_COS_APPID=1234567890

# Optional: CDN domain for faster file access
# TENCENT_COS_CDN_DOMAIN=cdn.yourdomain.com

# ==================== LLM Service (Hunyuan) ====================
# Tencent Hunyuan API
TENCENT_HUNYUAN_SECRET_ID=${TENCENT_SECRET_ID}
TENCENT_HUNYUAN_SECRET_KEY=${TENCENT_SECRET_KEY}

# Main LLM configuration
MAIN_LLM=hunyuan  # Use Tencent Hunyuan as primary LLM
MAIN_LLM_MODEL=hunyuan-pro  # Options: hunyuan-lite, hunyuan-standard, hunyuan-pro

# Alternative models (optional)
# MAIN_LLM_MODEL=hunyuan-lite  # Fastest, most cost-effective
# MAIN_LLM_MODEL=hunyuan-standard  # Balanced performance

# ==================== SMS Service ====================
# Tencent Cloud SMS
TENCENT_SMS_SECRET_ID=${TENCENT_SECRET_ID}
TENCENT_SMS_SECRET_KEY=${TENCENT_SECRET_KEY}
TENCENT_SMS_APP_ID=your_sms_app_id
TENCENT_SMS_SIGN_NAME=your_sms_signature
TENCENT_SMS_TEMPLATE_ID=123456

# ==================== Email Service ====================
# Tencent Cloud SES (Simple Email Service)
TENCENT_EMAIL_SECRET_ID=${TENCENT_SECRET_ID}
TENCENT_EMAIL_SECRET_KEY=${TENCENT_SECRET_KEY}
TENCENT_EMAIL_REGION=ap-guangzhou
TENCENT_EMAIL_FROM=noreply@yourdomain.com
TENCENT_EMAIL_FROM_NAME=Kortix

# ==================== Sandbox Configuration ====================
# Code execution sandbox provider
SANDBOX_PROVIDER=docker  # Options: docker | e2b

# ==================== Authentication ====================
# JWT configuration for self-hosted auth
JWT_SECRET=your_jwt_secret_key_at_least_32_chars
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ==================== Redis (Optional - for caching) ====================
# Use Tencent Redis or local Redis
REDIS_HOST=your-redis.redis.tencentcdb.com
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
REDIS_DB=0

# ==================== Payment (China) ====================
# WeChat Pay Configuration
WECHAT_PAY_APP_ID=your_wechat_app_id
WECHAT_PAY_MCH_ID=your_merchant_id
WECHAT_PAY_API_KEY=your_api_key
WECHAT_PAY_CERT_PATH=/path/to/cert.pem
WECHAT_PAY_KEY_PATH=/path/to/key.pem

# Alipay Configuration (optional)
ALIPAY_APP_ID=your_alipay_app_id
ALIPAY_PRIVATE_KEY=your_alipay_private_key
ALIPAY_PUBLIC_KEY=alipay_public_key

# ==================== Application Settings ====================
# Backend URL
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Environment mode
ENV_MODE=production  # Options: local | staging | production

# ==================== Optional: Mixed Cloud ====================
# You can mix Tencent Cloud with other services if needed

# Use Tencent COS but keep Supabase for auth (during migration)
# DATABASE_PROVIDER=tencent
# STORAGE_PROVIDER=tencent_cos
# AUTH_PROVIDER=supabase  # Temporary during migration

# ==================== Monitoring & Logging ====================
# Tencent Cloud CLS (Cloud Log Service) - optional
# TENCENT_CLS_SECRET_ID=${TENCENT_SECRET_ID}
# TENCENT_CLS_SECRET_KEY=${TENCENT_SECRET_KEY}
# TENCENT_CLS_REGION=ap-guangzhou
# TENCENT_CLS_LOGSET_ID=your-logset-id
# TENCENT_CLS_TOPIC_ID=your-topic-id

# ==================================================================
# Quick Start Checklist:
# ==================================================================
# 1. [ ] Create Tencent Cloud account and get Secret ID/Key
# 2. [ ] Create TDSQL-C or PostgreSQL instance
# 3. [ ] Create COS bucket
# 4. [ ] Get Hunyuan API access
# 5. [ ] Set up Redis (optional but recommended)
# 6. [ ] Configure SMS signature and template
# 7. [ ] Set up payment (WeChat Pay/Alipay)
# 8. [ ] Update DNS and domain (for production)
# 9. [ ] Enable CDN for COS (optional, for better performance)
# 10. [ ] Set up monitoring and alerts
# ==================================================================

# For detailed setup guide, see: docs/DEPLOYMENT_CHINA.md
